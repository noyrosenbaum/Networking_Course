#include <stdio.h>
#include <sys/time.h>

#define TIMEOUT 10 // milliseconds - ENSURE IN CODE THE TIME UNITS

int main()
{
    int timer = 0;
    printf("hello partb");
    while (timer < 10)
    {
        recv();
        timer = 0seconds;
    }
    send("timeout")

        return 0;
}