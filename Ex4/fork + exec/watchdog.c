#include <stdio.h>
#include <sys/time.h>

#define TIMEOUT 10 // milliseconds - ENSURE IN CODE THE TIME UNITS

int main()
{


    return 0;
}

//Staff's code
/*
    int timer = 0;
    while (timer < 10)
    {
        recv();
        timer = 0seconds;
    }
    send("timeout")
*/